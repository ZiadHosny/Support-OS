{
  "_comment": "Request bodies for mutating operations, keyed by operationId (NODE-2). Each NODE story adds the fixtures for the module it ports. Bodies here are deliberately INVALID-but-well-formed where a valid one would have a side effect: the harness compares the two services' rejections, which exercises the same validation and error-shape path without mutating real rows. Only --mutating uses these.",
  "auth_token_create": { "email": "admin@supportos.local", "password": "deliberately-wrong-so-both-sides-401" },
  "auth_token_refresh_create": { "refresh": "not-a-real-token" },
  "auth_token_verify_mfa_create": { "mfa_token": "not-a-real-challenge", "code": "000000" },
  "auth_logout_create": { "refresh": "not-a-real-token" },
  "auth_change_password_create": { "current_password": "deliberately-wrong", "new_password": "Irrelevant!2026" },
  "auth_invite_confirm_create": { "token": "not-a-real-invite", "password": "Irrelevant!2026" },
  "auth_password_reset_request_create": { "email": "definitely-not-registered@example.invalid" },
  "auth_password_reset_confirm_create": { "token": "not-a-real-reset", "password": "Irrelevant!2026" },
  "auth_2fa_enroll_create": {},
  "auth_2fa_confirm_create": { "code": "000000" },
  "auth_2fa_disable_create": { "password": "deliberately-wrong" }
}
