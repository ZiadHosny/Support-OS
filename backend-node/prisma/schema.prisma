generator client {
  provider        = "prisma-client"
  // Under src/ so `tsconfig.build.json`'s rootDir ("./src") includes it in
  // the compiled build — a sibling output dir outside src/ is invisible to
  // `nest build`. Generated, gitignored (backend-node/.gitignore).
  output          = "../src/generated/prisma"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model accounts_auditlog {
  id                                                            BigInt              @id @default(autoincrement())
  created_at                                                    DateTime            @db.Timestamptz(6)
  updated_at                                                    DateTime            @db.Timestamptz(6)
  action                                                        String              @db.VarChar(30)
  target_label                                                  String              @db.VarChar(150)
  from_value                                                    String
  to_value                                                      String
  actor_id                                                      BigInt?
  target_role_id                                                BigInt?
  target_user_id                                                BigInt?
  target_customer_id                                            BigInt?
  accounts_user_accounts_auditlog_actor_idToaccounts_user       accounts_user?      @relation("accounts_auditlog_actor_idToaccounts_user", fields: [actor_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_auditlog_actor_id_f3348b18_fk_accounts_user_id")
  customers_customer                                            customers_customer? @relation(fields: [target_customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_auditlog_target_customer_id_dd219aac_fk_customers")
  accounts_role                                                 accounts_role?      @relation(fields: [target_role_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_auditlog_target_role_id_15f3859d_fk_accounts_role_id")
  accounts_user_accounts_auditlog_target_user_idToaccounts_user accounts_user?      @relation("accounts_auditlog_target_user_idToaccounts_user", fields: [target_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_auditlog_target_user_id_2f24470d_fk_accounts_user_id")

  @@index([actor_id], map: "accounts_auditlog_actor_id_f3348b18")
  @@index([created_at], map: "accounts_auditlog_created_at_1cf54f27")
  @@index([target_customer_id], map: "accounts_auditlog_target_customer_id_dd219aac")
  @@index([target_role_id], map: "accounts_auditlog_target_role_id_15f3859d")
  @@index([target_user_id], map: "accounts_auditlog_target_user_id_2f24470d")
}

model accounts_role {
  id                  BigInt              @id @default(autoincrement())
  created_at          DateTime            @db.Timestamptz(6)
  updated_at          DateTime            @db.Timestamptz(6)
  slug                String              @unique @db.VarChar(50)
  name                String              @db.VarChar(100)
  description         String              @db.VarChar(255)
  permissions         Json
  is_system           Boolean
  requires_two_factor Boolean
  accounts_auditlog   accounts_auditlog[]
  accounts_user       accounts_user[]

  @@index([created_at], map: "accounts_role_created_at_b371308a")
  @@index([slug], map: "accounts_role_slug_df8fb6a2_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model accounts_twofactorrecoverycode {
  id            BigInt        @id @default(autoincrement())
  created_at    DateTime      @db.Timestamptz(6)
  updated_at    DateTime      @db.Timestamptz(6)
  code_hash     String        @db.VarChar(64)
  used_at       DateTime?     @db.Timestamptz(6)
  user_id       BigInt
  accounts_user accounts_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_twofactorre_user_id_efccc721_fk_accounts_")

  @@index([created_at], map: "accounts_twofactorrecoverycode_created_at_1e70ec55")
  @@index([user_id], map: "accounts_twofactorrecoverycode_user_id_efccc721")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model accounts_user {
  id                                                                   BigInt                                @id @default(autoincrement())
  password                                                             String                                @db.VarChar(128)
  last_login                                                           DateTime?                             @db.Timestamptz(6)
  is_superuser                                                         Boolean
  email                                                                String                                @unique @db.VarChar(254)
  first_name                                                           String                                @db.VarChar(150)
  last_name                                                            String                                @db.VarChar(150)
  is_active                                                            Boolean
  is_staff                                                             Boolean
  date_joined                                                          DateTime                              @db.Timestamptz(6)
  role_id                                                              BigInt?
  department_id                                                        BigInt?
  branch_id                                                            BigInt?
  mfa_enabled                                                          Boolean
  mfa_secret                                                           String                                @db.VarChar(255)
  accounts_auditlog_accounts_auditlog_actor_idToaccounts_user          accounts_auditlog[]                   @relation("accounts_auditlog_actor_idToaccounts_user")
  accounts_auditlog_accounts_auditlog_target_user_idToaccounts_user    accounts_auditlog[]                   @relation("accounts_auditlog_target_user_idToaccounts_user")
  accounts_twofactorrecoverycode                                       accounts_twofactorrecoverycode[]
  organization_branch                                                  organization_branch?                  @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_branch_id_38ec6caf_fk_organization_branch_id")
  organization_department                                              organization_department?              @relation(fields: [department_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_department_id_8dc06840_fk_organizat")
  accounts_role                                                        accounts_role?                        @relation(fields: [role_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_role_id_a6dd19b0_fk_accounts_role_id")
  accounts_user_groups                                                 accounts_user_groups[]
  accounts_user_user_permissions                                       accounts_user_user_permissions[]
  agents_internalnote                                                  agents_internalnote[]
  agents_internalnote_mentioned_users                                  agents_internalnote_mentioned_users[]
  agents_task                                                          agents_task[]
  customers_attachment                                                 customers_attachment[]
  customers_customer                                                   customers_customer?
  customers_note                                                       customers_note[]
  django_admin_log                                                     django_admin_log[]
  integrations_apikey_integrations_apikey_created_by_idToaccounts_user integrations_apikey[]                 @relation("integrations_apikey_created_by_idToaccounts_user")
  integrations_apikey_integrations_apikey_user_idToaccounts_user       integrations_apikey[]                 @relation("integrations_apikey_user_idToaccounts_user")
  integrations_erpsyncrun                                              integrations_erpsyncrun[]
  integrations_webhooksubscription                                     integrations_webhooksubscription[]
  notifications_notification                                           notifications_notification[]
  sla_assignmentrule                                                   sla_assignmentrule[]
  sla_assignmentrule_agents                                            sla_assignmentrule_agents[]
  tickets_savedview                                                    tickets_savedview?
  tickets_ticket                                                       tickets_ticket[]
  tickets_ticketactivity                                               tickets_ticketactivity[]
  token_blacklist_outstandingtoken                                     token_blacklist_outstandingtoken[]

  @@index([branch_id], map: "accounts_user_branch_id_38ec6caf")
  @@index([department_id], map: "accounts_user_department_id_8dc06840")
  @@index([email], map: "accounts_user_email_b2644a56_like")
  @@index([role_id], map: "accounts_user_role_id_a6dd19b0")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model accounts_user_groups {
  id            BigInt        @id @default(autoincrement())
  user_id       BigInt
  group_id      Int
  auth_group    auth_group    @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_groups_group_id_bd11a704_fk_auth_group_id")
  accounts_user accounts_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_groups_user_id_52b62117_fk_accounts_user_id")

  @@unique([user_id, group_id], map: "accounts_user_groups_user_id_group_id_59c0b32f_uniq")
  @@index([group_id], map: "accounts_user_groups_group_id_bd11a704")
  @@index([user_id], map: "accounts_user_groups_user_id_52b62117")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model accounts_user_user_permissions {
  id              BigInt          @id @default(autoincrement())
  user_id         BigInt
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_user_p_permission_id_113bb443_fk_auth_perm")
  accounts_user   accounts_user   @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "accounts_user_user_p_user_id_e4f0a161_fk_accounts_")

  @@unique([user_id, permission_id], map: "accounts_user_user_permi_user_id_permission_id_2ab516c2_uniq")
  @@index([permission_id], map: "accounts_user_user_permissions_permission_id_113bb443")
  @@index([user_id], map: "accounts_user_user_permissions_user_id_e4f0a161")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model agents_internalnote {
  id                                  BigInt                                @id @default(autoincrement())
  created_at                          DateTime                              @db.Timestamptz(6)
  updated_at                          DateTime                              @db.Timestamptz(6)
  body                                String
  author_id                           BigInt?
  ticket_id                           BigInt
  accounts_user                       accounts_user?                        @relation(fields: [author_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_internalnote_author_id_cb73d877_fk_accounts_user_id")
  tickets_ticket                      tickets_ticket                        @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_internalnote_ticket_id_4ab70653_fk_tickets_ticket_id")
  agents_internalnote_mentioned_users agents_internalnote_mentioned_users[]

  @@index([author_id], map: "agents_internalnote_author_id_cb73d877")
  @@index([created_at], map: "agents_internalnote_created_at_6e151124")
  @@index([ticket_id], map: "agents_internalnote_ticket_id_4ab70653")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model agents_internalnote_mentioned_users {
  id                  BigInt              @id @default(autoincrement())
  internalnote_id     BigInt
  user_id             BigInt
  agents_internalnote agents_internalnote @relation(fields: [internalnote_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_internalnote__internalnote_id_47db603f_fk_agents_in")
  accounts_user       accounts_user       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_internalnote__user_id_fe409c08_fk_accounts_")

  @@unique([internalnote_id, user_id], map: "agents_internalnote_ment_internalnote_id_user_id_c1da5f92_uniq")
  @@index([internalnote_id], map: "agents_internalnote_mentioned_users_internalnote_id_47db603f")
  @@index([user_id], map: "agents_internalnote_mentioned_users_user_id_fe409c08")
}

model agents_quickreply {
  id         BigInt   @id @default(autoincrement())
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  title      String   @db.VarChar(200)
  body       String

  @@index([created_at], map: "agents_quickreply_created_at_d5c7e471")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model agents_task {
  id               BigInt          @id @default(autoincrement())
  created_at       DateTime        @db.Timestamptz(6)
  updated_at       DateTime        @db.Timestamptz(6)
  title            String          @db.VarChar(255)
  description      String          @db.VarChar(1000)
  due_at           DateTime        @db.Timestamptz(6)
  completed_at     DateTime?       @db.Timestamptz(6)
  reminder_sent_at DateTime?       @db.Timestamptz(6)
  owner_id         BigInt
  ticket_id        BigInt?
  accounts_user    accounts_user   @relation(fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_task_owner_id_32e50f11_fk_accounts_user_id")
  tickets_ticket   tickets_ticket? @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "agents_task_ticket_id_ae926563_fk_tickets_ticket_id")

  @@index([created_at], map: "agents_task_created_at_887f057b")
  @@index([owner_id], map: "agents_task_owner_id_32e50f11")
  @@index([ticket_id], map: "agents_task_ticket_id_ae926563")
  @@index([due_at], map: "task_due_at_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model ai_chatbotsession {
  id             BigInt         @id @default(autoincrement())
  created_at     DateTime       @db.Timestamptz(6)
  updated_at     DateTime       @db.Timestamptz(6)
  handed_off_at  DateTime?      @db.Timestamptz(6)
  ticket_id      BigInt         @unique
  tickets_ticket tickets_ticket @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "ai_chatbotsession_ticket_id_fce9a7be_fk_tickets_ticket_id")

  @@index([created_at], map: "ai_chatbotsession_created_at_f52fdc93")
}

model auth_group {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique @db.VarChar(150)
  accounts_user_groups   accounts_user_groups[]
  auth_group_permissions auth_group_permissions[]

  @@index([name], map: "auth_group_name_a6ea08ec_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_group_permissions {
  id              BigInt          @id @default(autoincrement())
  group_id        Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  auth_group      auth_group      @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([group_id, permission_id], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([group_id], map: "auth_group_permissions_group_id_b120cbf9")
  @@index([permission_id], map: "auth_group_permissions_permission_id_84c5c92e")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_permission {
  id                             Int                              @id @default(autoincrement())
  name                           String                           @db.VarChar(255)
  content_type_id                Int
  codename                       String                           @db.VarChar(100)
  accounts_user_user_permissions accounts_user_user_permissions[]
  auth_group_permissions         auth_group_permissions[]
  django_content_type            django_content_type              @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")

  @@unique([content_type_id, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
  @@index([content_type_id], map: "auth_permission_content_type_id_2f476e4b")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model communications_emailproviderconfig {
  id                 BigInt   @id @default(autoincrement())
  created_at         DateTime @db.Timestamptz(6)
  updated_at         DateTime @db.Timestamptz(6)
  host               String   @db.VarChar(255)
  port               Int
  host_user          String   @db.VarChar(255)
  host_password      String   @db.VarChar(255)
  use_tls            Boolean
  default_from_email String   @db.VarChar(254)

  @@index([created_at], map: "communications_emailproviderconfig_created_at_bfad5f60")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model communications_message {
  id             BigInt         @id @default(autoincrement())
  created_at     DateTime       @db.Timestamptz(6)
  updated_at     DateTime       @db.Timestamptz(6)
  direction      String         @db.VarChar(10)
  channel        String         @db.VarChar(20)
  body           String
  metadata       Json
  ticket_id      BigInt
  target_address String         @db.VarChar(254)
  tickets_ticket tickets_ticket @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "communications_message_ticket_id_e3cad2dd_fk_tickets_ticket_id")

  @@index([created_at], map: "communications_message_created_at_04913f11")
  @@index([ticket_id], map: "communications_message_ticket_id_e3cad2dd")
}

model communications_smsproviderconfig {
  id           BigInt   @id @default(autoincrement())
  created_at   DateTime @db.Timestamptz(6)
  updated_at   DateTime @db.Timestamptz(6)
  api_base_url String   @db.VarChar(500)
  account_sid  String   @db.VarChar(100)
  auth_token   String   @db.VarChar(500)
  from_number  String   @db.VarChar(40)

  @@index([created_at], map: "communications_smsproviderconfig_created_at_570dbb4e")
}

model communications_whatsappproviderconfig {
  id              BigInt   @id @default(autoincrement())
  created_at      DateTime @db.Timestamptz(6)
  updated_at      DateTime @db.Timestamptz(6)
  api_base_url    String   @db.VarChar(500)
  phone_number_id String   @db.VarChar(100)
  access_token    String   @db.VarChar(500)

  @@index([created_at], map: "communications_whatsappproviderconfig_created_at_2d7bfbb7")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model customers_attachment {
  id                 BigInt             @id @default(autoincrement())
  created_at         DateTime           @db.Timestamptz(6)
  updated_at         DateTime           @db.Timestamptz(6)
  file               String             @db.VarChar(100)
  original_filename  String             @db.VarChar(255)
  size               Int
  customer_id        BigInt
  uploaded_by_id     BigInt?
  customers_customer customers_customer @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_attachment_customer_id_ec4c2264_fk_customers")
  accounts_user      accounts_user?     @relation(fields: [uploaded_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_attachment_uploaded_by_id_ff3ccdcf_fk_accounts_")

  @@index([created_at], map: "customers_attachment_created_at_31189980")
  @@index([customer_id], map: "customers_attachment_customer_id_ec4c2264")
  @@index([uploaded_by_id], map: "customers_attachment_uploaded_by_id_ff3ccdcf")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model customers_contactdetail {
  id                 BigInt             @id @default(autoincrement())
  created_at         DateTime           @db.Timestamptz(6)
  updated_at         DateTime           @db.Timestamptz(6)
  channel            String             @db.VarChar(20)
  value              String             @db.VarChar(254)
  customer_id        BigInt
  customers_customer customers_customer @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_contactdet_customer_id_89b66faa_fk_customers")

  @@unique([customer_id, channel, value], map: "unique_customer_channel_value")
  @@index([created_at], map: "customers_contactdetail_created_at_44b4c89e")
  @@index([customer_id], map: "customers_contactdetail_customer_id_89b66faa")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model customers_customer {
  id                      BigInt                    @id @default(autoincrement())
  created_at              DateTime                  @db.Timestamptz(6)
  updated_at              DateTime                  @db.Timestamptz(6)
  name                    String                    @db.VarChar(200)
  email                   String?                   @unique @db.VarChar(254)
  phone                   String                    @db.VarChar(40)
  company                 String                    @db.VarChar(200)
  user_id                 BigInt?                   @unique
  external_id             String?                   @unique @db.VarChar(100)
  branch_id               BigInt?
  email_contact_enabled   Boolean
  phone_contact_enabled   Boolean
  whatsapp_enabled        Boolean
  legal_hold              Boolean
  accounts_auditlog       accounts_auditlog[]
  customers_attachment    customers_attachment[]
  customers_contactdetail customers_contactdetail[]
  organization_branch     organization_branch?      @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_customer_branch_id_6f75e537_fk_organization_branch_id")
  accounts_user           accounts_user?            @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_customer_user_id_a9568d6c_fk_accounts_user_id")
  customers_note          customers_note[]
  integrations_erporder   integrations_erporder[]
  tickets_feedback        tickets_feedback[]
  tickets_ticket          tickets_ticket[]

  @@index([name], map: "customer_name_idx")
  @@index([name(ops: raw("gin_trgm_ops"))], map: "customer_name_trgm", type: Gin)
  @@index([branch_id], map: "customers_customer_branch_id_6f75e537")
  @@index([created_at], map: "customers_customer_created_at_b0f7c38c")
  @@index([email], map: "customers_customer_email_300e404b_like")
  @@index([external_id], map: "customers_customer_external_id_b1779fc4_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model customers_note {
  id                 BigInt             @id @default(autoincrement())
  created_at         DateTime           @db.Timestamptz(6)
  updated_at         DateTime           @db.Timestamptz(6)
  body               String
  author_id          BigInt?
  customer_id        BigInt
  accounts_user      accounts_user?     @relation(fields: [author_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_note_author_id_269b3f5d_fk_accounts_user_id")
  customers_customer customers_customer @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "customers_note_customer_id_505ed79b_fk_customers_customer_id")

  @@index([author_id], map: "customers_note_author_id_269b3f5d")
  @@index([created_at], map: "customers_note_created_at_a562254e")
  @@index([customer_id], map: "customers_note_customer_id_505ed79b")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_admin_log {
  id                  Int                  @id @default(autoincrement())
  action_time         DateTime             @db.Timestamptz(6)
  object_id           String?
  object_repr         String               @db.VarChar(200)
  action_flag         Int                  @db.SmallInt
  change_message      String
  content_type_id     Int?
  user_id             BigInt
  django_content_type django_content_type? @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  accounts_user       accounts_user        @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_user_id_c564eba6_fk_accounts_user_id")

  @@index([content_type_id], map: "django_admin_log_content_type_id_c4bce8eb")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6")
}

model django_celery_beat_clockedschedule {
  id                              Int                               @id @default(autoincrement())
  clocked_time                    DateTime                          @db.Timestamptz(6)
  django_celery_beat_periodictask django_celery_beat_periodictask[]
}

model django_celery_beat_crontabschedule {
  id                              Int                               @id @default(autoincrement())
  minute                          String                            @db.VarChar(240)
  hour                            String                            @db.VarChar(96)
  day_of_week                     String                            @db.VarChar(64)
  day_of_month                    String                            @db.VarChar(124)
  month_of_year                   String                            @db.VarChar(64)
  timezone                        String                            @db.VarChar(63)
  django_celery_beat_periodictask django_celery_beat_periodictask[]
}

model django_celery_beat_intervalschedule {
  id                              Int                               @id @default(autoincrement())
  every                           Int
  period                          String                            @db.VarChar(24)
  django_celery_beat_periodictask django_celery_beat_periodictask[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_celery_beat_periodictask {
  id                                  Int                                  @id @default(autoincrement())
  name                                String                               @unique @db.VarChar(200)
  task                                String                               @db.VarChar(200)
  args                                String
  kwargs                              String
  queue                               String?                              @db.VarChar(200)
  exchange                            String?                              @db.VarChar(200)
  routing_key                         String?                              @db.VarChar(200)
  expires                             DateTime?                            @db.Timestamptz(6)
  enabled                             Boolean
  last_run_at                         DateTime?                            @db.Timestamptz(6)
  total_run_count                     Int
  date_changed                        DateTime                             @db.Timestamptz(6)
  description                         String
  crontab_id                          Int?
  interval_id                         Int?
  solar_id                            Int?
  one_off                             Boolean
  start_time                          DateTime?                            @db.Timestamptz(6)
  priority                            Int?
  headers                             String
  clocked_id                          Int?
  expire_seconds                      Int?
  django_celery_beat_clockedschedule  django_celery_beat_clockedschedule?  @relation(fields: [clocked_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_celery_beat_p_clocked_id_47a69f82_fk_django_ce")
  django_celery_beat_crontabschedule  django_celery_beat_crontabschedule?  @relation(fields: [crontab_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_celery_beat_p_crontab_id_d3cba168_fk_django_ce")
  django_celery_beat_intervalschedule django_celery_beat_intervalschedule? @relation(fields: [interval_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_celery_beat_p_interval_id_a8ca27da_fk_django_ce")
  django_celery_beat_solarschedule    django_celery_beat_solarschedule?    @relation(fields: [solar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_celery_beat_p_solar_id_a87ce72c_fk_django_ce")

  @@index([clocked_id], map: "django_celery_beat_periodictask_clocked_id_47a69f82")
  @@index([crontab_id], map: "django_celery_beat_periodictask_crontab_id_d3cba168")
  @@index([interval_id], map: "django_celery_beat_periodictask_interval_id_a8ca27da")
  @@index([name], map: "django_celery_beat_periodictask_name_265a36b7_like")
  @@index([solar_id], map: "django_celery_beat_periodictask_solar_id_a87ce72c")
}

model django_celery_beat_periodictasks {
  ident       Int      @id @db.SmallInt
  last_update DateTime @db.Timestamptz(6)
}

model django_celery_beat_solarschedule {
  id                              Int                               @id @default(autoincrement())
  event                           String                            @db.VarChar(24)
  latitude                        Decimal                           @db.Decimal(9, 6)
  longitude                       Decimal                           @db.Decimal(9, 6)
  django_celery_beat_periodictask django_celery_beat_periodictask[]

  @@unique([event, latitude, longitude], map: "django_celery_beat_solar_event_latitude_longitude_ba64999a_uniq")
}

model django_content_type {
  id               Int                @id @default(autoincrement())
  app_label        String             @db.VarChar(100)
  model            String             @db.VarChar(100)
  auth_permission  auth_permission[]
  django_admin_log django_admin_log[]

  @@unique([app_label, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
}

model django_migrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)
}

model django_session {
  session_key  String   @id @db.VarChar(40)
  session_data String
  expire_date  DateTime @db.Timestamptz(6)

  @@index([expire_date], map: "django_session_expire_date_a5c62663")
  @@index([session_key], map: "django_session_session_key_c0390e0f_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model integrations_apikey {
  id                                                             BigInt         @id @default(autoincrement())
  created_at                                                     DateTime       @db.Timestamptz(6)
  updated_at                                                     DateTime       @db.Timestamptz(6)
  name                                                           String         @db.VarChar(100)
  prefix                                                         String         @unique @db.VarChar(12)
  hashed_key                                                     String         @db.VarChar(64)
  is_active                                                      Boolean
  expires_at                                                     DateTime?      @db.Timestamptz(6)
  last_used_at                                                   DateTime?      @db.Timestamptz(6)
  created_by_id                                                  BigInt?
  user_id                                                        BigInt
  accounts_user_integrations_apikey_created_by_idToaccounts_user accounts_user? @relation("integrations_apikey_created_by_idToaccounts_user", fields: [created_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_apikey_created_by_id_47d9cfad_fk_accounts_user_id")
  accounts_user_integrations_apikey_user_idToaccounts_user       accounts_user  @relation("integrations_apikey_user_idToaccounts_user", fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_apikey_user_id_9c946c62_fk_accounts_user_id")

  @@index([created_at], map: "integrations_apikey_created_at_1bafce64")
  @@index([created_by_id], map: "integrations_apikey_created_by_id_47d9cfad")
  @@index([prefix], map: "integrations_apikey_prefix_d6b87be4_like")
  @@index([user_id], map: "integrations_apikey_user_id_9c946c62")
}

model integrations_erpconnection {
  id                         BigInt    @id @default(autoincrement())
  created_at                 DateTime  @db.Timestamptz(6)
  updated_at                 DateTime  @db.Timestamptz(6)
  enabled                    Boolean
  base_url                   String    @db.VarChar(500)
  auth_token                 String    @db.VarChar(500)
  export_enabled             Boolean
  customer_field_map         Json
  order_field_map            Json
  customer_external_id_field String    @db.VarChar(100)
  order_external_id_field    String    @db.VarChar(100)
  order_customer_ref_field   String    @db.VarChar(100)
  last_sync_at               DateTime? @db.Timestamptz(6)

  @@index([created_at], map: "integrations_erpconnection_created_at_5ced782a")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model integrations_erporder {
  id                 BigInt             @id @default(autoincrement())
  created_at         DateTime           @db.Timestamptz(6)
  updated_at         DateTime           @db.Timestamptz(6)
  external_id        String             @unique @db.VarChar(100)
  order_number       String             @db.VarChar(100)
  status             String             @db.VarChar(50)
  total_amount       Decimal?           @db.Decimal(14, 2)
  currency           String             @db.VarChar(3)
  placed_at          DateTime?          @db.Timestamptz(6)
  raw                Json
  synced_at          DateTime           @db.Timestamptz(6)
  customer_id        BigInt
  customers_customer customers_customer @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_erporde_customer_id_4f0e3aa7_fk_customers")

  @@index([placed_at(sort: Desc), id(sort: Desc)], map: "erporder_placed_idx")
  @@index([created_at], map: "integrations_erporder_created_at_0d25a385")
  @@index([customer_id], map: "integrations_erporder_customer_id_4f0e3aa7")
  @@index([external_id], map: "integrations_erporder_external_id_d8401d53_like")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model integrations_erpsyncrun {
  id              BigInt         @id @default(autoincrement())
  created_at      DateTime       @db.Timestamptz(6)
  updated_at      DateTime       @db.Timestamptz(6)
  direction       String         @db.VarChar(10)
  state           String         @db.VarChar(10)
  created_count   Int
  updated_count   Int
  skipped_count   Int
  failed_count    Int
  started_at      DateTime       @db.Timestamptz(6)
  finished_at     DateTime?      @db.Timestamptz(6)
  error_message   String
  triggered_by_id BigInt?
  accounts_user   accounts_user? @relation(fields: [triggered_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_erpsync_triggered_by_id_1ac4ae54_fk_accounts_")

  @@index([started_at(sort: Desc)], map: "erpsyncrun_started_idx")
  @@index([created_at], map: "integrations_erpsyncrun_created_at_c9059da0")
  @@index([triggered_by_id], map: "integrations_erpsyncrun_triggered_by_id_1ac4ae54")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model integrations_webhookdelivery {
  id                               BigInt                           @id @default(autoincrement())
  created_at                       DateTime                         @db.Timestamptz(6)
  updated_at                       DateTime                         @db.Timestamptz(6)
  event                            String                           @db.VarChar(50)
  payload                          Json
  state                            String                           @db.VarChar(10)
  attempt                          Int
  response_status_code             Int?
  response_body                    String
  error_message                    String
  subscription_id                  BigInt
  integrations_webhooksubscription integrations_webhooksubscription @relation(fields: [subscription_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_webhook_subscription_id_f51730e0_fk_integrati")

  @@index([created_at], map: "integrations_webhookdelivery_created_at_d3453962")
  @@index([subscription_id], map: "integrations_webhookdelivery_subscription_id_f51730e0")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model integrations_webhooksubscription {
  id                           BigInt                         @id @default(autoincrement())
  created_at                   DateTime                       @db.Timestamptz(6)
  updated_at                   DateTime                       @db.Timestamptz(6)
  name                         String                         @db.VarChar(100)
  target_url                   String                         @db.VarChar(500)
  secret                       String                         @db.VarChar(255)
  events                       Json
  enabled                      Boolean
  created_by_id                BigInt?
  integrations_webhookdelivery integrations_webhookdelivery[]
  accounts_user                accounts_user?                 @relation(fields: [created_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "integrations_webhook_created_by_id_29392b22_fk_accounts_")

  @@index([created_at], map: "integrations_webhooksubscription_created_at_3c6371c2")
  @@index([created_by_id], map: "integrations_webhooksubscription_created_by_id_29392b22")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model knowledge_base_article {
  id                      BigInt                   @id @default(autoincrement())
  created_at              DateTime                 @db.Timestamptz(6)
  updated_at              DateTime                 @db.Timestamptz(6)
  title_en                String                   @db.VarChar(200)
  title_ar                String                   @db.VarChar(200)
  body_en                 String
  body_ar                 String
  status                  String                   @db.VarChar(20)
  category_id             BigInt?
  knowledge_base_category knowledge_base_category? @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "knowledge_base_artic_category_id_e33c041b_fk_knowledge")

  @@index([category_id], map: "knowledge_base_article_category_id_e33c041b")
  @@index([created_at], map: "knowledge_base_article_created_at_b78db404")
}

model knowledge_base_category {
  id                     BigInt                   @id @default(autoincrement())
  created_at             DateTime                 @db.Timestamptz(6)
  updated_at             DateTime                 @db.Timestamptz(6)
  name                   String                   @unique @db.VarChar(100)
  color                  String                   @db.VarChar(7)
  knowledge_base_article knowledge_base_article[]

  @@index([created_at], map: "knowledge_base_category_created_at_9ad338a2")
  @@index([name], map: "knowledge_base_category_name_3b8e18c5_like")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model knowledge_base_faq {
  id         BigInt   @id @default(autoincrement())
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  question   String   @db.VarChar(300)
  answer     String
  order      Int

  @@index([created_at], map: "knowledge_base_faq_created_at_88b20906")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model notifications_notification {
  id             BigInt          @id @default(autoincrement())
  created_at     DateTime        @db.Timestamptz(6)
  updated_at     DateTime        @db.Timestamptz(6)
  kind           String          @db.VarChar(30)
  title          String          @db.VarChar(255)
  body           String          @db.VarChar(500)
  read_at        DateTime?       @db.Timestamptz(6)
  email_sent_at  DateTime?       @db.Timestamptz(6)
  recipient_id   BigInt
  ticket_id      BigInt?
  accounts_user  accounts_user   @relation(fields: [recipient_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "notifications_notifi_recipient_id_d055f3f0_fk_accounts_")
  tickets_ticket tickets_ticket? @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "notifications_notifi_ticket_id_422ab224_fk_tickets_t")

  @@index([created_at], map: "notifications_notification_created_at_10160fdd")
  @@index([recipient_id], map: "notifications_notification_recipient_id_d055f3f0")
  @@index([ticket_id], map: "notifications_notification_ticket_id_422ab224")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model organization_branch {
  id                            BigInt                         @id @default(autoincrement())
  created_at                    DateTime                       @db.Timestamptz(6)
  updated_at                    DateTime                       @db.Timestamptz(6)
  name                          String                         @unique @db.VarChar(100)
  description                   String                         @db.VarChar(255)
  calendar_id                   BigInt?
  accounts_user                 accounts_user[]
  customers_customer            customers_customer[]
  organization_businesscalendar organization_businesscalendar? @relation(fields: [calendar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "organization_branch_calendar_id_9184cf8f_fk_organizat")
  tickets_ticket                tickets_ticket[]

  @@index([calendar_id], map: "organization_branch_calendar_id_9184cf8f")
  @@index([created_at], map: "organization_branch_created_at_440e490d")
  @@index([name], map: "organization_branch_name_89427b2f_like")
}

model organization_businesscalendar {
  id                         BigInt                       @id @default(autoincrement())
  created_at                 DateTime                     @db.Timestamptz(6)
  updated_at                 DateTime                     @db.Timestamptz(6)
  name                       String                       @unique @db.VarChar(100)
  description                String                       @db.VarChar(255)
  organization_branch        organization_branch[]
  organization_holiday       organization_holiday[]
  organization_workingwindow organization_workingwindow[]
  sla_slapolicy              sla_slapolicy[]

  @@index([created_at], map: "organization_businesscalendar_created_at_1e68e487")
  @@index([name], map: "organization_businesscalendar_name_d0e322c0_like")
}

model organization_department {
  id             BigInt           @id @default(autoincrement())
  created_at     DateTime         @db.Timestamptz(6)
  updated_at     DateTime         @db.Timestamptz(6)
  name           String           @unique @db.VarChar(100)
  description    String           @db.VarChar(255)
  accounts_user  accounts_user[]
  tickets_ticket tickets_ticket[]

  @@index([created_at], map: "organization_department_created_at_0453d4e3")
  @@index([name], map: "organization_department_name_fe51166b_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model organization_holiday {
  id                            BigInt                        @id @default(autoincrement())
  created_at                    DateTime                      @db.Timestamptz(6)
  updated_at                    DateTime                      @db.Timestamptz(6)
  date                          DateTime                      @db.Date
  label                         String                        @db.VarChar(100)
  calendar_id                   BigInt
  organization_businesscalendar organization_businesscalendar @relation(fields: [calendar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "organization_holiday_calendar_id_237aa33e_fk_organizat")

  @@unique([calendar_id, date], map: "unique_holiday_calendar_date")
  @@index([calendar_id], map: "organization_holiday_calendar_id_237aa33e")
  @@index([created_at], map: "organization_holiday_created_at_ff406f3e")
}

model organization_landingcontent {
  id                          BigInt   @id @default(autoincrement())
  created_at                  DateTime @db.Timestamptz(6)
  updated_at                  DateTime @db.Timestamptz(6)
  hero_headline_en            String   @db.VarChar(200)
  hero_headline_ar            String   @db.VarChar(200)
  hero_value_proposition_en   String
  hero_value_proposition_ar   String
  hero_primary_cta_label_en   String   @db.VarChar(60)
  hero_primary_cta_label_ar   String   @db.VarChar(60)
  hero_primary_cta_target     String   @db.VarChar(20)
  hero_secondary_cta_label_en String   @db.VarChar(60)
  hero_secondary_cta_label_ar String   @db.VarChar(60)
  hero_secondary_cta_target   String   @db.VarChar(20)
  features_title_en           String   @db.VarChar(200)
  features_title_ar           String   @db.VarChar(200)
  cta_title_en                String   @db.VarChar(200)
  cta_title_ar                String   @db.VarChar(200)
  cta_subtitle_en             String
  cta_subtitle_ar             String
  cta_label_en                String   @db.VarChar(60)
  cta_label_ar                String   @db.VarChar(60)
  cta_target                  String   @db.VarChar(20)
  footer_text_en              String   @db.VarChar(200)
  footer_text_ar              String   @db.VarChar(200)
  hero_image_url              String   @db.VarChar(500)

  @@index([created_at], map: "organization_landingcontent_created_at_f16284e4")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model organization_landinghighlight {
  id             BigInt   @id @default(autoincrement())
  created_at     DateTime @db.Timestamptz(6)
  updated_at     DateTime @db.Timestamptz(6)
  title_en       String   @db.VarChar(120)
  title_ar       String   @db.VarChar(120)
  description_en String
  description_ar String
  icon           String   @db.VarChar(32)
  order          Int

  @@index([created_at], map: "organization_landinghighlight_created_at_536aceaf")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model organization_landingsociallink {
  id         BigInt   @id @default(autoincrement())
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  platform   String   @unique(map: "unique_landing_social_platform") @db.VarChar(20)
  value      String   @db.VarChar(254)
  is_enabled Boolean
  order      Int

  @@index([created_at], map: "organization_landingsociallink_created_at_d265da00")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model organization_organizationsettings {
  id                                BigInt   @id @default(autoincrement())
  created_at                        DateTime @db.Timestamptz(6)
  updated_at                        DateTime @db.Timestamptz(6)
  name                              String   @db.VarChar(150)
  logo_url                          String   @db.VarChar(500)
  default_response_target_minutes   Int?
  default_resolution_target_minutes Int?
  primary_color                     String   @db.VarChar(7)
  retention_attachments_days        Int?
  retention_audit_log_days          Int?
  retention_closed_tickets_days     Int?
  retention_messages_days           Int?

  @@index([created_at], map: "organization_organizationsettings_created_at_fbeb5067")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model organization_workingwindow {
  id                            BigInt                        @id @default(autoincrement())
  created_at                    DateTime                      @db.Timestamptz(6)
  updated_at                    DateTime                      @db.Timestamptz(6)
  weekday                       Int                           @db.SmallInt
  start_time                    DateTime                      @db.Time(6)
  end_time                      DateTime                      @db.Time(6)
  calendar_id                   BigInt
  organization_businesscalendar organization_businesscalendar @relation(fields: [calendar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "organization_working_calendar_id_c140ad56_fk_organizat")

  @@unique([calendar_id, weekday], map: "unique_working_window_calendar_weekday")
  @@index([calendar_id], map: "organization_workingwindow_calendar_id_c140ad56")
  @@index([created_at], map: "organization_workingwindow_created_at_f23ceb3a")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model sla_assignmentrule {
  id                        BigInt                      @id @default(autoincrement())
  created_at                DateTime                    @db.Timestamptz(6)
  updated_at                DateTime                    @db.Timestamptz(6)
  strategy                  String                      @db.VarChar(20)
  enabled                   Boolean
  category_id               BigInt?                     @unique(map: "unique_assignment_rule_category")
  last_assigned_agent_id    BigInt?
  tickets_category          tickets_category?           @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_assignmentrule_category_id_e739136f_fk_tickets_category_id")
  accounts_user             accounts_user?              @relation(fields: [last_assigned_agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_assignmentrule_last_assigned_agent__a57d5ff8_fk_accounts_")
  sla_assignmentrule_agents sla_assignmentrule_agents[]

  @@index([category_id], map: "sla_assignmentrule_category_id_e739136f")
  @@index([created_at], map: "sla_assignmentrule_created_at_ddef53be")
  @@index([last_assigned_agent_id], map: "sla_assignmentrule_last_assigned_agent_id_a57d5ff8")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model sla_assignmentrule_agents {
  id                 BigInt             @id @default(autoincrement())
  assignmentrule_id  BigInt
  user_id            BigInt
  sla_assignmentrule sla_assignmentrule @relation(fields: [assignmentrule_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_assignmentrule_a_assignmentrule_id_7f202e7d_fk_sla_assig")
  accounts_user      accounts_user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_assignmentrule_agents_user_id_44ab505a_fk_accounts_user_id")

  @@unique([assignmentrule_id, user_id], map: "sla_assignmentrule_agent_assignmentrule_id_user_i_78a099f4_uniq")
  @@index([assignmentrule_id], map: "sla_assignmentrule_agents_assignmentrule_id_7f202e7d")
  @@index([user_id], map: "sla_assignmentrule_agents_user_id_44ab505a")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model sla_escalationrule {
  id                BigInt   @id @default(autoincrement())
  created_at        DateTime @db.Timestamptz(6)
  updated_at        DateTime @db.Timestamptz(6)
  kind              String   @db.VarChar(20)
  threshold_minutes Int
  enabled           Boolean

  @@index([created_at], map: "sla_escalationrule_created_at_76ef9149")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model sla_slapolicy {
  id                            BigInt                         @id @default(autoincrement())
  created_at                    DateTime                       @db.Timestamptz(6)
  updated_at                    DateTime                       @db.Timestamptz(6)
  priority                      String                         @db.VarChar(20)
  response_target_minutes       Int
  resolution_target_minutes     Int
  category_id                   BigInt?
  calendar_id                   BigInt?
  organization_businesscalendar organization_businesscalendar? @relation(fields: [calendar_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_slapolicy_calendar_id_ee5d4bef_fk_organizat")
  tickets_category              tickets_category?              @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sla_slapolicy_category_id_b4284842_fk_tickets_category_id")

  @@unique([priority, category_id], map: "unique_sla_policy_priority_category")
  @@index([calendar_id], map: "sla_slapolicy_calendar_id_ee5d4bef")
  @@index([category_id], map: "sla_slapolicy_category_id_b4284842")
  @@index([created_at], map: "sla_slapolicy_created_at_2e259b7d")
}

model tickets_category {
  id                 BigInt              @id @default(autoincrement())
  created_at         DateTime            @db.Timestamptz(6)
  updated_at         DateTime            @db.Timestamptz(6)
  name               String              @unique @db.VarChar(100)
  sla_assignmentrule sla_assignmentrule?
  sla_slapolicy      sla_slapolicy[]
  tickets_ticket     tickets_ticket[]

  @@index([created_at], map: "tickets_category_created_at_5f0841a2")
  @@index([name], map: "tickets_category_name_11b65d5e_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model tickets_feedback {
  id                 BigInt             @id @default(autoincrement())
  created_at         DateTime           @db.Timestamptz(6)
  updated_at         DateTime           @db.Timestamptz(6)
  rating             String             @db.VarChar(20)
  comment            String
  customer_id        BigInt
  ticket_id          BigInt             @unique
  customers_customer customers_customer @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_feedback_customer_id_9d1bcffc_fk_customers_customer_id")
  tickets_ticket     tickets_ticket     @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_feedback_ticket_id_6b728aa8_fk_tickets_ticket_id")

  @@index([created_at], map: "tickets_feedback_created_at_77d3ebad")
  @@index([customer_id], map: "tickets_feedback_customer_id_9d1bcffc")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model tickets_savedview {
  id            BigInt        @id @default(autoincrement())
  created_at    DateTime      @db.Timestamptz(6)
  updated_at    DateTime      @db.Timestamptz(6)
  name          String        @db.VarChar(100)
  is_shared     Boolean
  filters       Json
  is_default    Boolean
  owner_id      BigInt        @unique(map: "unique_saved_view_owner_default", where: raw("is_default"))
  accounts_user accounts_user @relation(fields: [owner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_savedview_owner_id_776a2e82_fk_accounts_user_id")

  @@unique([owner_id, name], map: "unique_saved_view_owner_name")
  @@index([created_at], map: "tickets_savedview_created_at_924dc093")
  @@index([owner_id], map: "tickets_savedview_owner_id_776a2e82")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model tickets_ticket {
  id                         BigInt                       @id @default(autoincrement())
  created_at                 DateTime                     @db.Timestamptz(6)
  updated_at                 DateTime                     @db.Timestamptz(6)
  subject                    String                       @db.VarChar(200)
  description                String
  status                     String                       @db.VarChar(20)
  priority                   String                       @db.VarChar(20)
  customer_id                BigInt
  category_id                BigInt?
  assigned_agent_id          BigInt?
  escalated                  Boolean
  escalated_at               DateTime?                    @db.Timestamptz(6)
  department_id              BigInt?
  branch_id                  BigInt?
  merged_into_id             BigInt?
  closed_at                  DateTime?                    @db.Timestamptz(6)
  pending_customer_since     DateTime?                    @db.Timestamptz(6)
  sla_paused_minutes         Int
  agents_internalnote        agents_internalnote[]
  agents_task                agents_task[]
  ai_chatbotsession          ai_chatbotsession?
  communications_message     communications_message[]
  notifications_notification notifications_notification[]
  tickets_feedback           tickets_feedback?
  accounts_user              accounts_user?               @relation(fields: [assigned_agent_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_assigned_agent_id_6025e539_fk_accounts_user_id")
  organization_branch        organization_branch?         @relation(fields: [branch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_branch_id_5773ac24_fk_organization_branch_id")
  tickets_category           tickets_category?            @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_category_id_710dbfd0_fk_tickets_category_id")
  customers_customer         customers_customer           @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_customer_id_f2d9135a_fk_customers_customer_id")
  organization_department    organization_department?     @relation(fields: [department_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_department_id_32e39a8e_fk_organizat")
  tickets_ticket             tickets_ticket?              @relation("tickets_ticketTotickets_ticket", fields: [merged_into_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticket_merged_into_id_3a36ad40_fk_tickets_ticket_id")
  other_tickets_ticket       tickets_ticket[]             @relation("tickets_ticketTotickets_ticket")
  tickets_ticketactivity     tickets_ticketactivity[]

  @@index([priority, created_at(sort: Desc)], map: "ticket_priority_created_idx")
  @@index([status, created_at(sort: Desc)], map: "ticket_status_created_idx")
  @@index([subject(ops: raw("gin_trgm_ops"))], map: "ticket_subject_trgm", type: Gin)
  @@index([assigned_agent_id], map: "tickets_ticket_assigned_agent_id_6025e539")
  @@index([branch_id], map: "tickets_ticket_branch_id_5773ac24")
  @@index([category_id], map: "tickets_ticket_category_id_710dbfd0")
  @@index([created_at], map: "tickets_ticket_created_at_95bb8d8e")
  @@index([customer_id], map: "tickets_ticket_customer_id_f2d9135a")
  @@index([department_id], map: "tickets_ticket_department_id_32e39a8e")
  @@index([merged_into_id], map: "tickets_ticket_merged_into_id_3a36ad40")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model tickets_ticketactivity {
  id             BigInt         @id @default(autoincrement())
  created_at     DateTime       @db.Timestamptz(6)
  updated_at     DateTime       @db.Timestamptz(6)
  kind           String         @db.VarChar(20)
  from_value     String         @db.VarChar(150)
  to_value       String         @db.VarChar(150)
  actor_id       BigInt?
  ticket_id      BigInt
  accounts_user  accounts_user? @relation(fields: [actor_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticketactivity_actor_id_f1254c51_fk_accounts_user_id")
  tickets_ticket tickets_ticket @relation(fields: [ticket_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "tickets_ticketactivity_ticket_id_655a7928_fk_tickets_ticket_id")

  @@index([actor_id], map: "tickets_ticketactivity_actor_id_f1254c51")
  @@index([created_at], map: "tickets_ticketactivity_created_at_7e156f47")
  @@index([ticket_id], map: "tickets_ticketactivity_ticket_id_655a7928")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model token_blacklist_blacklistedtoken {
  id                               BigInt                           @id @default(autoincrement())
  blacklisted_at                   DateTime                         @db.Timestamptz(6)
  token_id                         BigInt                           @unique
  token_blacklist_outstandingtoken token_blacklist_outstandingtoken @relation(fields: [token_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "token_blacklist_blacklistedtoken_token_id_3cc7fe56_fk")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model token_blacklist_outstandingtoken {
  id                               BigInt                            @id @default(autoincrement())
  token                            String
  created_at                       DateTime?                         @db.Timestamptz(6)
  expires_at                       DateTime                          @db.Timestamptz(6)
  user_id                          BigInt?
  jti                              String                            @unique(map: "token_blacklist_outstandingtoken_jti_hex_d9bdf6f7_uniq") @db.VarChar(255)
  token_blacklist_blacklistedtoken token_blacklist_blacklistedtoken?
  accounts_user                    accounts_user?                    @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "token_blacklist_outs_user_id_83bc629a_fk_accounts_")

  @@index([jti], map: "token_blacklist_outstandingtoken_jti_hex_d9bdf6f7_like")
  @@index([user_id], map: "token_blacklist_outstandingtoken_user_id_83bc629a")
}
