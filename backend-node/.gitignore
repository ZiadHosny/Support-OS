# compiled output
/dist
/node_modules
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store

# Generated by `npx prisma generate` — see prisma/schema.prisma's
# `generator client { output = "../src/generated/prisma" }`.
/src/generated/prisma

# contract-diff's --json output (NODE-2) — a generated artefact, not a
# checked-in report. docs/BACKEND-ARCHITECTURE.md's "generated, not
# checked in" instinct applies here too.
/contract-diff-report.json
